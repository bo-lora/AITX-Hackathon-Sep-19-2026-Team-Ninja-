#!/bin/sh
# openemr CLI wrapper. Uses the Playwright + tsx install inside ../auth-broker/node_modules (symlinked as ./node_modules).
DIR="$(cd "$(dirname "$0")" && pwd -P)"
export PLAYWRIGHT_BROWSERS_PATH="${PLAYWRIGHT_BROWSERS_PATH:-0}"
exec "$DIR/node_modules/.bin/tsx" "$DIR/openemr.ts" "$@"
