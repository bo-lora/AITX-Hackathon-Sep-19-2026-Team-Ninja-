# ContextNinja Chrome extension

Load unpacked from this folder in `chrome://extensions`. Create skill records DOM events in the current tab. Done POSTs JSON to `http://127.0.0.1:43123/api/sessions` and opens the workflow page.

No API keys. The extension never calls Grok.
